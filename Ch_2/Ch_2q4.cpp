#include <iostream>
using namespace std;

//다 비교하는 걸까 의심했음...
int main() {
    double a, b, c, d, f;
    double max;
    cout << "5개의 실수를 입력하시오>>";
    cin >> a >> b >> c >> d >> f;
}

/*
int main() {
    double a, b, c, d, f;
    cout << "5개의 실수를 입력하시오>>";
    cin >> a >> b >> c >> d >> f;

    double max = a;
    if(b > max) max = b;
    if(c > max) max = c;
    if(d > max) max = d;
    if(f > max) max = f;

    cout << "제일 큰 수 = " << max;
}
*/